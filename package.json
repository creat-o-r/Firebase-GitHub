{
  "name": "firebase-github-cicd",
  "version": "1.0.0",
  "description": "Universal CI/CD infrastructure with GitHub App",
  "main": "github-app/app.js",
  "scripts": {
    "start": "cd github-app && npm start",
    "dev": "cd github-app && npm run dev",
    "install-app": "cd github-app && npm install"
  },
  "dependencies": {
    "@octokit/webhooks": "^12.0.0",
    "@octokit/auth-app": "^6.0.0",
    "@octokit/rest": "^20.0.0",
    "express": "^4.18.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}